# The MySQL database server configuration file.
#
# {{ ansible_managed }}
{% for configGroup in mysql.config.global %}
[{{ (configGroup|dict2items)[0]['key']|string }}]
{% for settings, valueList in configGroup.items() %}
{% for valuePair in valueList %}
{% for setting in valuePair|dict2items %}
{{ setting.key|string + '=' + setting.value|string }}
{% endfor %}
{% endfor %}
{% endfor %}
{% endfor %}
